# Example configuration for GOAD OpenStack deployment
# Copy this file to terraform.tfvars and customize as needed

# Number of complete GOAD instances to deploy
# Each instance includes: 5 Windows VMs + 1 Ubuntu deployment box + 1 Kali box
# For example:
#   goad_instance_count = 1   -> 1 GOAD lab  (5 Windows + 1 Ubuntu + 1 Kali)
#   goad_instance_count = 2   -> 2 GOAD labs (10 Windows + 2 Ubuntu + 2 Kali)
#   goad_instance_count = 10  -> 10 GOAD labs (50 Windows + 10 Ubuntu + 10 Kali)
#   goad_instance_count = 30  -> 30 GOAD labs (150 Windows + 30 Ubuntu + 30 Kali)
goad_instance_count = 30

# Image names (use: openstack image list)
# Windows Server 2019 is used for DC01, DC02, and SRV02
windows_server_2019_image = "WindowsServer2019"
# Windows Server 2016 is used for DC03 and SRV03
windows_server_2016_image = "WindowsServer2016"
ubuntu_image_name         = "ubuntu-noble-server"
kali_image_name           = "Kali2025"

# Flavor names (use: openstack flavor list)
# Domain Controllers need more resources
dc_flavor_name     = "medium.6gb"  # 6GB RAM, 2 VCPUs - for DC01, DC02, DC03
server_flavor_name = "medium.6gb"  # 6GB RAM, 2 VCPUs - for SRV02, SRV03
deploy_flavor_name = "medium"      # 4GB RAM, 2 VCPUs - for Ubuntu deployment boxes (one per GOAD network)
kali_flavor_name   = "large"       # 8GB RAM, 4 VCPUs - for Kali boxes (one per GOAD network)

# SSH keypair name (use: openstack keypair list)
# REQUIRED: Set this to your OpenStack SSH keypair name
keypair = "CHANGEME"

# External network for floating IPs
external_network = "MAIN-NAT"

# Enable/disable floating IPs for external access
enable_floating_ips = true

# Disk sizes (in GB)
windows_disk_size = 80
linux_disk_size   = 50
